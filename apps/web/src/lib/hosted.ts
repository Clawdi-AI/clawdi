// Build-time flag, statically replaced by Next.js. See `hosted/README.md`
// for the directory contract (data-hosted marker, no top-level side effects).
export const IS_HOSTED = process.env.NEXT_PUBLIC_CLAWDI_HOSTED === "true";
