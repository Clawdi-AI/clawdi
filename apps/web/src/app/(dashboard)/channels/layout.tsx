export const metadata = {
	title: "Channels",
	description: "Connect messaging bots to agents and manage paired external chats.",
};

export default function ChannelsLayout({ children }: { children: React.ReactNode }) {
	return children;
}
