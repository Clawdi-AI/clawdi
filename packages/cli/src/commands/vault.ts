import chalk from "chalk";

export async function vaultSet(key: string) {
	console.log(chalk.yellow(`TODO: vault set ${key}`));
}

export async function vaultList() {
	console.log(chalk.yellow("TODO: vault list"));
}

export async function vaultImport(file: string) {
	console.log(chalk.yellow(`TODO: vault import ${file}`));
}
