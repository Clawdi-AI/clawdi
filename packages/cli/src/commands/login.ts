import chalk from "chalk";

export async function login() {
	console.log(chalk.yellow("TODO: open browser for auth, save ApiKey to ~/.clawdi/auth.json"));
}

export async function logout() {
	console.log(chalk.yellow("TODO: clear ~/.clawdi/auth.json"));
}
