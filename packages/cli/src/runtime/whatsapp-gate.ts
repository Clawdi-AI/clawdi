// Managed WhatsApp adapters remain bundle-only. Activation is blocked until
// fixed runtime artifacts can be installed and both runtimes expose a public
// stable identity for retry-safe arbitrary outbound delivery.
export const WHATSAPP_UPSTREAM_READY = false;
