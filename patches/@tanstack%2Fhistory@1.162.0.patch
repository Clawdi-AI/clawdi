diff --git a/dist/cjs/index.cjs b/dist/cjs/index.cjs
index 8f88a73c..e94026f4 100644
--- a/dist/cjs/index.cjs
+++ b/dist/cjs/index.cjs
@@ -177 +177 @@ function createBrowserHistory(opts) {
-	let ignoreNextPop = false;
+	let popTransaction;
@@ -206,0 +207,7 @@ function createBrowserHistory(opts) {
+	const sameRouterEntry = (a, b) => {
+		const aIndex = a.state[stateIndexKey];
+		const bIndex = b.state[stateIndexKey];
+		const aKey = a.state.__TSR_key ?? a.state.key;
+		const bKey = b.state.__TSR_key ?? b.state.key;
+		return Number.isFinite(aIndex) && Number.isFinite(bIndex) && typeof aKey === "string" && a.href === b.href && aIndex === bIndex && aKey === bKey;
+	};
@@ -208,2 +215,25 @@ function createBrowserHistory(opts) {
-		if (ignoreNextPop) {
-			ignoreNextPop = false;
+		const popWasGo = nextPopIsGo;
+		nextPopIsGo = false;
+		const nextLocation = parseLocation();
+		const skipBlocker = skipBlockerNextPop;
+		skipBlockerNextPop = false;
+		const transaction = popTransaction;
+		if (transaction) {
+			transaction.target = nextLocation;
+			transaction.sawGo ||= popWasGo;
+			if (transaction.phase === "deciding") return;
+			if (sameRouterEntry(nextLocation, transaction.origin)) {
+				currentLocation = nextLocation;
+				popTransaction = void 0;
+				return;
+			}
+			if (transaction.phase === "rolling-back-fallback") {
+				popTransaction = void 0;
+				return;
+			}
+			const rollbackDelta = transaction.origin.state[stateIndexKey] - nextLocation.state[stateIndexKey];
+			if (Number.isFinite(rollbackDelta) && rollbackDelta !== 0) win.history.go(rollbackDelta);
+			else {
+				transaction.phase = "rolling-back-fallback";
+				win.history.go(1);
+			}
@@ -212,13 +242,27 @@ function createBrowserHistory(opts) {
-		const nextLocation = parseLocation();
-		const delta = nextLocation.state[stateIndexKey] - currentLocation.state[stateIndexKey];
-		const isForward = delta === 1;
-		const isBack = delta === -1;
-		const isGo = !isForward && !isBack || nextPopIsGo;
-		nextPopIsGo = false;
-		const action = isGo ? "GO" : isBack ? "BACK" : "FORWARD";
-		const notify = isGo ? {
-			type: "GO",
-			index: delta
-		} : { type: isBack ? "BACK" : "FORWARD" };
-		if (skipBlockerNextPop) skipBlockerNextPop = false;
-		else {
+		const origin = currentLocation;
+		const pendingTransaction = {
+			origin,
+			target: nextLocation,
+			phase: "deciding",
+			sawGo: popWasGo
+		};
+		popTransaction = pendingTransaction;
+		const navigation = () => {
+			const target = pendingTransaction.target;
+			const delta = target.state[stateIndexKey] - origin.state[stateIndexKey];
+			const isForward = delta === 1;
+			const isBack = delta === -1;
+			const isGo = pendingTransaction.sawGo || !isForward && !isBack;
+			const action = isGo ? "GO" : isBack ? "BACK" : "FORWARD";
+			const notify = isGo ? {
+				type: "GO",
+				index: delta
+			} : { type: isBack ? "BACK" : "FORWARD" };
+			return {
+				action,
+				delta,
+				notify,
+				target
+			};
+		};
+		if (!skipBlocker) {
@@ -227,9 +271,24 @@ function createBrowserHistory(opts) {
-				for (const blocker of blockers) if (await blocker.blockerFn({
-					currentLocation,
-					nextLocation,
-					action
-				})) {
-					ignoreNextPop = true;
-					win.history.go(1);
-					history.notify(notify);
-					return;
+				for (const blocker of blockers) {
+					const { action, target } = navigation();
+					const isBlocked = await blocker.blockerFn({
+						currentLocation: origin,
+						nextLocation: target,
+						action
+					});
+					if (popTransaction !== pendingTransaction) return;
+					if (isBlocked) {
+						const { delta, notify, target: target$1 } = navigation();
+						if (sameRouterEntry(target$1, origin)) {
+							currentLocation = target$1;
+							popTransaction = void 0;
+							return;
+						} else if (Number.isFinite(delta) && delta !== 0) {
+							pendingTransaction.phase = "rolling-back";
+							win.history.go(-delta);
+						} else {
+							pendingTransaction.phase = "rolling-back-fallback";
+							win.history.go(1);
+						}
+						history.notify(notify);
+						return;
+					}
@@ -239 +298,8 @@ function createBrowserHistory(opts) {
-		currentLocation = parseLocation();
+		const { notify, target } = navigation();
+		if (sameRouterEntry(target, origin)) {
+			currentLocation = target;
+			popTransaction = void 0;
+			return;
+		}
+		currentLocation = target;
+		popTransaction = void 0;
@@ -286,0 +353 @@ function createBrowserHistory(opts) {
+			popTransaction = void 0;
diff --git a/dist/esm/index.js b/dist/esm/index.js
index f109cf46..a59bff53 100644
--- a/dist/esm/index.js
+++ b/dist/esm/index.js
@@ -176 +176 @@ function createBrowserHistory(opts) {
-	let ignoreNextPop = false;
+	let popTransaction;
@@ -205,0 +206,7 @@ function createBrowserHistory(opts) {
+	const sameRouterEntry = (a, b) => {
+		const aIndex = a.state[stateIndexKey];
+		const bIndex = b.state[stateIndexKey];
+		const aKey = a.state.__TSR_key ?? a.state.key;
+		const bKey = b.state.__TSR_key ?? b.state.key;
+		return Number.isFinite(aIndex) && Number.isFinite(bIndex) && typeof aKey === "string" && a.href === b.href && aIndex === bIndex && aKey === bKey;
+	};
@@ -207,2 +214,25 @@ function createBrowserHistory(opts) {
-		if (ignoreNextPop) {
-			ignoreNextPop = false;
+		const popWasGo = nextPopIsGo;
+		nextPopIsGo = false;
+		const nextLocation = parseLocation();
+		const skipBlocker = skipBlockerNextPop;
+		skipBlockerNextPop = false;
+		const transaction = popTransaction;
+		if (transaction) {
+			transaction.target = nextLocation;
+			transaction.sawGo ||= popWasGo;
+			if (transaction.phase === "deciding") return;
+			if (sameRouterEntry(nextLocation, transaction.origin)) {
+				currentLocation = nextLocation;
+				popTransaction = void 0;
+				return;
+			}
+			if (transaction.phase === "rolling-back-fallback") {
+				popTransaction = void 0;
+				return;
+			}
+			const rollbackDelta = transaction.origin.state[stateIndexKey] - nextLocation.state[stateIndexKey];
+			if (Number.isFinite(rollbackDelta) && rollbackDelta !== 0) win.history.go(rollbackDelta);
+			else {
+				transaction.phase = "rolling-back-fallback";
+				win.history.go(1);
+			}
@@ -211,13 +241,27 @@ function createBrowserHistory(opts) {
-		const nextLocation = parseLocation();
-		const delta = nextLocation.state[stateIndexKey] - currentLocation.state[stateIndexKey];
-		const isForward = delta === 1;
-		const isBack = delta === -1;
-		const isGo = !isForward && !isBack || nextPopIsGo;
-		nextPopIsGo = false;
-		const action = isGo ? "GO" : isBack ? "BACK" : "FORWARD";
-		const notify = isGo ? {
-			type: "GO",
-			index: delta
-		} : { type: isBack ? "BACK" : "FORWARD" };
-		if (skipBlockerNextPop) skipBlockerNextPop = false;
-		else {
+		const origin = currentLocation;
+		const pendingTransaction = {
+			origin,
+			target: nextLocation,
+			phase: "deciding",
+			sawGo: popWasGo
+		};
+		popTransaction = pendingTransaction;
+		const navigation = () => {
+			const target = pendingTransaction.target;
+			const delta = target.state[stateIndexKey] - origin.state[stateIndexKey];
+			const isForward = delta === 1;
+			const isBack = delta === -1;
+			const isGo = pendingTransaction.sawGo || !isForward && !isBack;
+			const action = isGo ? "GO" : isBack ? "BACK" : "FORWARD";
+			const notify = isGo ? {
+				type: "GO",
+				index: delta
+			} : { type: isBack ? "BACK" : "FORWARD" };
+			return {
+				action,
+				delta,
+				notify,
+				target
+			};
+		};
+		if (!skipBlocker) {
@@ -226,9 +270,24 @@ function createBrowserHistory(opts) {
-				for (const blocker of blockers) if (await blocker.blockerFn({
-					currentLocation,
-					nextLocation,
-					action
-				})) {
-					ignoreNextPop = true;
-					win.history.go(1);
-					history.notify(notify);
-					return;
+				for (const blocker of blockers) {
+					const { action, target } = navigation();
+					const isBlocked = await blocker.blockerFn({
+						currentLocation: origin,
+						nextLocation: target,
+						action
+					});
+					if (popTransaction !== pendingTransaction) return;
+					if (isBlocked) {
+						const { delta, notify, target: target$1 } = navigation();
+						if (sameRouterEntry(target$1, origin)) {
+							currentLocation = target$1;
+							popTransaction = void 0;
+							return;
+						} else if (Number.isFinite(delta) && delta !== 0) {
+							pendingTransaction.phase = "rolling-back";
+							win.history.go(-delta);
+						} else {
+							pendingTransaction.phase = "rolling-back-fallback";
+							win.history.go(1);
+						}
+						history.notify(notify);
+						return;
+					}
@@ -238 +297,8 @@ function createBrowserHistory(opts) {
-		currentLocation = parseLocation();
+		const { notify, target } = navigation();
+		if (sameRouterEntry(target, origin)) {
+			currentLocation = target;
+			popTransaction = void 0;
+			return;
+		}
+		currentLocation = target;
+		popTransaction = void 0;
@@ -285,0 +352 @@ function createBrowserHistory(opts) {
+			popTransaction = void 0;
diff --git a/src/index.ts b/src/index.ts
index 705dbf01..402c6fc8 100644
--- a/src/index.ts
+++ b/src/index.ts
@@ -324 +324,8 @@ export function createBrowserHistory(opts?: {
-  let ignoreNextPop = false
+  let popTransaction:
+    | {
+        origin: HistoryLocation
+        target: HistoryLocation
+        phase: 'deciding' | 'rolling-back' | 'rolling-back-fallback'
+        sawGo: boolean
+      }
+    | undefined
@@ -403,0 +411,15 @@ export function createBrowserHistory(opts?: {
+  const sameRouterEntry = (a: HistoryLocation, b: HistoryLocation) => {
+    const aIndex = a.state[stateIndexKey]
+    const bIndex = b.state[stateIndexKey]
+    const aKey = a.state.__TSR_key ?? a.state.key
+    const bKey = b.state.__TSR_key ?? b.state.key
+    return (
+      Number.isFinite(aIndex) &&
+      Number.isFinite(bIndex) &&
+      typeof aKey === 'string' &&
+      a.href === b.href &&
+      aIndex === bIndex &&
+      aKey === bKey
+    )
+  }
+
@@ -405,2 +427,34 @@ export function createBrowserHistory(opts?: {
-    if (ignoreNextPop) {
-      ignoreNextPop = false
+    const popWasGo = nextPopIsGo
+    nextPopIsGo = false
+    const nextLocation = parseLocation()
+    const skipBlocker = skipBlockerNextPop
+    skipBlockerNextPop = false
+
+    const transaction = popTransaction
+    if (transaction) {
+      transaction.target = nextLocation
+      transaction.sawGo ||= popWasGo
+      if (transaction.phase === 'deciding') {
+        return
+      }
+
+      if (sameRouterEntry(nextLocation, transaction.origin)) {
+        currentLocation = nextLocation
+        popTransaction = undefined
+        return
+      }
+
+      if (transaction.phase === 'rolling-back-fallback') {
+        popTransaction = undefined
+        return
+      }
+
+      const rollbackDelta =
+        transaction.origin.state[stateIndexKey] -
+        nextLocation.state[stateIndexKey]
+      if (Number.isFinite(rollbackDelta) && rollbackDelta !== 0) {
+        win.history.go(rollbackDelta)
+      } else {
+        transaction.phase = 'rolling-back-fallback'
+        win.history.go(1)
+      }
@@ -410,17 +464,22 @@ export function createBrowserHistory(opts?: {
-    const nextLocation = parseLocation()
-    const delta =
-      nextLocation.state[stateIndexKey] - currentLocation.state[stateIndexKey]
-    const isForward = delta === 1
-    const isBack = delta === -1
-    const isGo = (!isForward && !isBack) || nextPopIsGo
-    nextPopIsGo = false
-
-    const action = isGo ? 'GO' : isBack ? 'BACK' : 'FORWARD'
-    const notify: SubscriberHistoryAction = isGo
-      ? {
-          type: 'GO',
-          index: delta,
-        }
-      : {
-          type: isBack ? 'BACK' : 'FORWARD',
-        }
+    const origin = currentLocation
+    const pendingTransaction: NonNullable<typeof popTransaction> = {
+      origin,
+      target: nextLocation,
+      phase: 'deciding',
+      sawGo: popWasGo,
+    }
+    popTransaction = pendingTransaction
+
+    const navigation = () => {
+      const target = pendingTransaction.target
+      const delta =
+        target.state[stateIndexKey] - origin.state[stateIndexKey]
+      const isForward = delta === 1
+      const isBack = delta === -1
+      const isGo = pendingTransaction.sawGo || (!isForward && !isBack)
+      const action: HistoryAction = isGo ? 'GO' : isBack ? 'BACK' : 'FORWARD'
+      const notify: SubscriberHistoryAction = isGo
+        ? { type: 'GO', index: delta }
+        : { type: isBack ? 'BACK' : 'FORWARD' }
+      return { action, delta, notify, target }
+    }
@@ -428,3 +487 @@ export function createBrowserHistory(opts?: {
-    if (skipBlockerNextPop) {
-      skipBlockerNextPop = false
-    } else {
+    if (!skipBlocker) {
@@ -433,0 +491 @@ export function createBrowserHistory(opts?: {
+          const { action, target } = navigation()
@@ -435,2 +493,2 @@ export function createBrowserHistory(opts?: {
-            currentLocation,
-            nextLocation,
+            currentLocation: origin,
+            nextLocation: target,
@@ -438,0 +497 @@ export function createBrowserHistory(opts?: {
+          if (popTransaction !== pendingTransaction) return
@@ -440,2 +499,12 @@ export function createBrowserHistory(opts?: {
-            ignoreNextPop = true
-            win.history.go(1)
+            const { delta, notify, target } = navigation()
+            if (sameRouterEntry(target, origin)) {
+              currentLocation = target
+              popTransaction = undefined
+              return
+            } else if (Number.isFinite(delta) && delta !== 0) {
+              pendingTransaction.phase = 'rolling-back'
+              win.history.go(-delta)
+            } else {
+              pendingTransaction.phase = 'rolling-back-fallback'
+              win.history.go(1)
+            }
@@ -449 +518,8 @@ export function createBrowserHistory(opts?: {
-    currentLocation = parseLocation()
+    const { notify, target } = navigation()
+    if (sameRouterEntry(target, origin)) {
+      currentLocation = target
+      popTransaction = undefined
+      return
+    }
+    currentLocation = target
+    popTransaction = undefined
@@ -509,0 +586 @@ export function createBrowserHistory(opts?: {
+      popTransaction = undefined
