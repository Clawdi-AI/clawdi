diff --git a/dist/cjs/index.cjs b/dist/cjs/index.cjs
--- a/dist/cjs/index.cjs
+++ b/dist/cjs/index.cjs
@@ -20,0 +21,8 @@
+		const isPushOrReplace = actionInfo.type === "PUSH" || actionInfo.type === "REPLACE";
+		if (typeof document !== "undefined" && isPushOrReplace) {
+			const pendingNavigation = opts.waitForNavigation?.();
+			if (pendingNavigation && !await pendingNavigation) {
+				opts.onBlocked?.();
+				return;
+			}
+		}
@@ -26,4 +34,2 @@
-		const isPushOrReplace = actionInfo.type === "PUSH" || actionInfo.type === "REPLACE";
-		if (typeof document !== "undefined" && blockers.length && isPushOrReplace) for (const blocker of blockers) {
-			const nextLocation = parseHref(actionInfo.path, actionInfo.state);
-			if (await blocker.blockerFn({
+		if (typeof document !== "undefined" && isPushOrReplace) {
+			const blockerArgs = {
@@ -31 +37 @@
-				nextLocation,
+				nextLocation: parseHref(actionInfo.path, actionInfo.state),
@@ -33 +39,12 @@
-			})) {
+			};
+			if (opts.runBlockers && blockers.length > 0) {
+				if (await opts.runBlockers(blockerArgs)) {
+					opts.onBlocked?.();
+					return;
+				}
+			} else for (const blocker of blockers) if (await blocker.blockerFn(blockerArgs)) {
+				opts.onBlocked?.();
+				return;
+			}
+			const pendingNavigation = opts.waitForNavigation?.();
+			if (pendingNavigation && !await pendingNavigation) {
@@ -177 +194 @@
-	let ignoreNextPop = false;
+	let popTransaction;
@@ -206,0 +224,31 @@
+	const sameRouterEntry = (a, b) => {
+		const aIndex = a.state[stateIndexKey];
+		const bIndex = b.state[stateIndexKey];
+		const aKey = a.state.__TSR_key ?? a.state.key;
+		const bKey = b.state.__TSR_key ?? b.state.key;
+		return Number.isFinite(aIndex) && Number.isFinite(bIndex) && typeof aKey === "string" && a.href === b.href && aIndex === bIndex && aKey === bKey;
+	};
+	let blockerDecisionPending = false;
+	const runBlockers = async (args) => {
+		const currentBlockers = _getBlockers();
+		if (typeof document === "undefined" || currentBlockers.length === 0) return false;
+		if (blockerDecisionPending) return true;
+		blockerDecisionPending = true;
+		try {
+			for (const blocker of currentBlockers) if (await blocker.blockerFn(args)) return true;
+			return false;
+		} finally {
+			blockerDecisionPending = false;
+		}
+	};
+	const finishPopTransaction = (transaction, result) => {
+		if (popTransaction !== transaction) return;
+		popTransaction = void 0;
+		transaction.finish(result);
+	};
+	const waitForNavigation = () => {
+		const transaction = popTransaction;
+		if (!transaction) return;
+		const owner = transaction.origin;
+		return transaction.completion.then((result) => result === "restored" && sameRouterEntry(currentLocation, owner));
+	};
@@ -208,2 +256,25 @@
-		if (ignoreNextPop) {
-			ignoreNextPop = false;
+		const popWasGo = nextPopIsGo;
+		nextPopIsGo = false;
+		const nextLocation = parseLocation();
+		const skipBlocker = skipBlockerNextPop;
+		skipBlockerNextPop = false;
+		const transaction = popTransaction;
+		if (transaction) {
+			transaction.target = nextLocation;
+			transaction.sawGo ||= popWasGo;
+			if (transaction.phase === "deciding") return;
+			if (sameRouterEntry(nextLocation, transaction.origin)) {
+				currentLocation = nextLocation;
+				finishPopTransaction(transaction, "restored");
+				return;
+			}
+			if (transaction.phase === "rolling-back-fallback") {
+				finishPopTransaction(transaction, "fallback");
+				return;
+			}
+			const rollbackDelta = transaction.origin.state[stateIndexKey] - nextLocation.state[stateIndexKey];
+			if (Number.isFinite(rollbackDelta) && rollbackDelta !== 0) win.history.go(rollbackDelta);
+			else {
+				transaction.phase = "rolling-back-fallback";
+				win.history.go(1);
+			}
@@ -212,23 +283,45 @@
-		const nextLocation = parseLocation();
-		const delta = nextLocation.state[stateIndexKey] - currentLocation.state[stateIndexKey];
-		const isForward = delta === 1;
-		const isBack = delta === -1;
-		const isGo = !isForward && !isBack || nextPopIsGo;
-		nextPopIsGo = false;
-		const action = isGo ? "GO" : isBack ? "BACK" : "FORWARD";
-		const notify = isGo ? {
-			type: "GO",
-			index: delta
-		} : { type: isBack ? "BACK" : "FORWARD" };
-		if (skipBlockerNextPop) skipBlockerNextPop = false;
-		else {
-			const blockers = _getBlockers();
-			if (typeof document !== "undefined" && blockers.length) {
-				for (const blocker of blockers) if (await blocker.blockerFn({
-					currentLocation,
-					nextLocation,
-					action
-				})) {
-					ignoreNextPop = true;
-					win.history.go(1);
-					history.notify(notify);
+		const origin = currentLocation;
+		let finish = () => {};
+		const completion = new Promise((resolve) => {
+			finish = resolve;
+		});
+		const pendingTransaction = {
+			origin,
+			target: nextLocation,
+			phase: "deciding",
+			sawGo: popWasGo,
+			completion,
+			finish
+		};
+		popTransaction = pendingTransaction;
+		const navigation = () => {
+			const target = pendingTransaction.target;
+			const delta = target.state[stateIndexKey] - origin.state[stateIndexKey];
+			const isForward = delta === 1;
+			const isBack = delta === -1;
+			const isGo = pendingTransaction.sawGo || !isForward && !isBack;
+			const action = isGo ? "GO" : isBack ? "BACK" : "FORWARD";
+			const notify = isGo ? {
+				type: "GO",
+				index: delta
+			} : { type: isBack ? "BACK" : "FORWARD" };
+			return {
+				action,
+				delta,
+				notify,
+				target
+			};
+		};
+		if (!skipBlocker) {
+			const { action, target } = navigation();
+			const isBlocked = await runBlockers({
+				currentLocation: origin,
+				nextLocation: target,
+				action
+			});
+			if (popTransaction !== pendingTransaction) return;
+			if (isBlocked) {
+				const { delta, target: target$1 } = navigation();
+				if (sameRouterEntry(target$1, origin)) {
+					currentLocation = target$1;
+					finishPopTransaction(pendingTransaction, "restored");
@@ -235,0 +329,6 @@
+				} else if (Number.isFinite(delta) && delta !== 0) {
+					pendingTransaction.phase = "rolling-back";
+					win.history.go(-delta);
+				} else {
+					pendingTransaction.phase = "rolling-back-fallback";
+					win.history.go(1);
@@ -236,0 +336 @@
+				return;
@@ -239 +339,7 @@
-		currentLocation = parseLocation();
+		const { notify, target } = navigation();
+		if (sameRouterEntry(target, origin)) {
+			currentLocation = target;
+			finishPopTransaction(pendingTransaction, "restored");
+			return;
+		}
+		currentLocation = target;
@@ -240,0 +347 @@
+		finishPopTransaction(pendingTransaction, "committed");
@@ -286,0 +394 @@
+			if (popTransaction) finishPopTransaction(popTransaction, "fallback");
@@ -296,0 +405,2 @@
+		runBlockers,
+		waitForNavigation,
diff --git a/dist/esm/index.js b/dist/esm/index.js
--- a/dist/esm/index.js
+++ b/dist/esm/index.js
@@ -19,0 +20,8 @@
+		const isPushOrReplace = actionInfo.type === "PUSH" || actionInfo.type === "REPLACE";
+		if (typeof document !== "undefined" && isPushOrReplace) {
+			const pendingNavigation = opts.waitForNavigation?.();
+			if (pendingNavigation && !await pendingNavigation) {
+				opts.onBlocked?.();
+				return;
+			}
+		}
@@ -25,4 +33,2 @@
-		const isPushOrReplace = actionInfo.type === "PUSH" || actionInfo.type === "REPLACE";
-		if (typeof document !== "undefined" && blockers.length && isPushOrReplace) for (const blocker of blockers) {
-			const nextLocation = parseHref(actionInfo.path, actionInfo.state);
-			if (await blocker.blockerFn({
+		if (typeof document !== "undefined" && isPushOrReplace) {
+			const blockerArgs = {
@@ -30 +36 @@
-				nextLocation,
+				nextLocation: parseHref(actionInfo.path, actionInfo.state),
@@ -32 +38,12 @@
-			})) {
+			};
+			if (opts.runBlockers && blockers.length > 0) {
+				if (await opts.runBlockers(blockerArgs)) {
+					opts.onBlocked?.();
+					return;
+				}
+			} else for (const blocker of blockers) if (await blocker.blockerFn(blockerArgs)) {
+				opts.onBlocked?.();
+				return;
+			}
+			const pendingNavigation = opts.waitForNavigation?.();
+			if (pendingNavigation && !await pendingNavigation) {
@@ -176 +193 @@
-	let ignoreNextPop = false;
+	let popTransaction;
@@ -205,0 +223,31 @@
+	const sameRouterEntry = (a, b) => {
+		const aIndex = a.state[stateIndexKey];
+		const bIndex = b.state[stateIndexKey];
+		const aKey = a.state.__TSR_key ?? a.state.key;
+		const bKey = b.state.__TSR_key ?? b.state.key;
+		return Number.isFinite(aIndex) && Number.isFinite(bIndex) && typeof aKey === "string" && a.href === b.href && aIndex === bIndex && aKey === bKey;
+	};
+	let blockerDecisionPending = false;
+	const runBlockers = async (args) => {
+		const currentBlockers = _getBlockers();
+		if (typeof document === "undefined" || currentBlockers.length === 0) return false;
+		if (blockerDecisionPending) return true;
+		blockerDecisionPending = true;
+		try {
+			for (const blocker of currentBlockers) if (await blocker.blockerFn(args)) return true;
+			return false;
+		} finally {
+			blockerDecisionPending = false;
+		}
+	};
+	const finishPopTransaction = (transaction, result) => {
+		if (popTransaction !== transaction) return;
+		popTransaction = void 0;
+		transaction.finish(result);
+	};
+	const waitForNavigation = () => {
+		const transaction = popTransaction;
+		if (!transaction) return;
+		const owner = transaction.origin;
+		return transaction.completion.then((result) => result === "restored" && sameRouterEntry(currentLocation, owner));
+	};
@@ -207,2 +255,25 @@
-		if (ignoreNextPop) {
-			ignoreNextPop = false;
+		const popWasGo = nextPopIsGo;
+		nextPopIsGo = false;
+		const nextLocation = parseLocation();
+		const skipBlocker = skipBlockerNextPop;
+		skipBlockerNextPop = false;
+		const transaction = popTransaction;
+		if (transaction) {
+			transaction.target = nextLocation;
+			transaction.sawGo ||= popWasGo;
+			if (transaction.phase === "deciding") return;
+			if (sameRouterEntry(nextLocation, transaction.origin)) {
+				currentLocation = nextLocation;
+				finishPopTransaction(transaction, "restored");
+				return;
+			}
+			if (transaction.phase === "rolling-back-fallback") {
+				finishPopTransaction(transaction, "fallback");
+				return;
+			}
+			const rollbackDelta = transaction.origin.state[stateIndexKey] - nextLocation.state[stateIndexKey];
+			if (Number.isFinite(rollbackDelta) && rollbackDelta !== 0) win.history.go(rollbackDelta);
+			else {
+				transaction.phase = "rolling-back-fallback";
+				win.history.go(1);
+			}
@@ -211,23 +282,45 @@
-		const nextLocation = parseLocation();
-		const delta = nextLocation.state[stateIndexKey] - currentLocation.state[stateIndexKey];
-		const isForward = delta === 1;
-		const isBack = delta === -1;
-		const isGo = !isForward && !isBack || nextPopIsGo;
-		nextPopIsGo = false;
-		const action = isGo ? "GO" : isBack ? "BACK" : "FORWARD";
-		const notify = isGo ? {
-			type: "GO",
-			index: delta
-		} : { type: isBack ? "BACK" : "FORWARD" };
-		if (skipBlockerNextPop) skipBlockerNextPop = false;
-		else {
-			const blockers = _getBlockers();
-			if (typeof document !== "undefined" && blockers.length) {
-				for (const blocker of blockers) if (await blocker.blockerFn({
-					currentLocation,
-					nextLocation,
-					action
-				})) {
-					ignoreNextPop = true;
-					win.history.go(1);
-					history.notify(notify);
+		const origin = currentLocation;
+		let finish = () => {};
+		const completion = new Promise((resolve) => {
+			finish = resolve;
+		});
+		const pendingTransaction = {
+			origin,
+			target: nextLocation,
+			phase: "deciding",
+			sawGo: popWasGo,
+			completion,
+			finish
+		};
+		popTransaction = pendingTransaction;
+		const navigation = () => {
+			const target = pendingTransaction.target;
+			const delta = target.state[stateIndexKey] - origin.state[stateIndexKey];
+			const isForward = delta === 1;
+			const isBack = delta === -1;
+			const isGo = pendingTransaction.sawGo || !isForward && !isBack;
+			const action = isGo ? "GO" : isBack ? "BACK" : "FORWARD";
+			const notify = isGo ? {
+				type: "GO",
+				index: delta
+			} : { type: isBack ? "BACK" : "FORWARD" };
+			return {
+				action,
+				delta,
+				notify,
+				target
+			};
+		};
+		if (!skipBlocker) {
+			const { action, target } = navigation();
+			const isBlocked = await runBlockers({
+				currentLocation: origin,
+				nextLocation: target,
+				action
+			});
+			if (popTransaction !== pendingTransaction) return;
+			if (isBlocked) {
+				const { delta, target: target$1 } = navigation();
+				if (sameRouterEntry(target$1, origin)) {
+					currentLocation = target$1;
+					finishPopTransaction(pendingTransaction, "restored");
@@ -234,0 +328,6 @@
+				} else if (Number.isFinite(delta) && delta !== 0) {
+					pendingTransaction.phase = "rolling-back";
+					win.history.go(-delta);
+				} else {
+					pendingTransaction.phase = "rolling-back-fallback";
+					win.history.go(1);
@@ -235,0 +335 @@
+				return;
@@ -238 +338,7 @@
-		currentLocation = parseLocation();
+		const { notify, target } = navigation();
+		if (sameRouterEntry(target, origin)) {
+			currentLocation = target;
+			finishPopTransaction(pendingTransaction, "restored");
+			return;
+		}
+		currentLocation = target;
@@ -239,0 +346 @@
+		finishPopTransaction(pendingTransaction, "committed");
@@ -285,0 +393 @@
+			if (popTransaction) finishPopTransaction(popTransaction, "fallback");
@@ -295,0 +404,2 @@
+		runBlockers,
+		waitForNavigation,
diff --git a/src/index.ts b/src/index.ts
--- a/src/index.ts
+++ b/src/index.ts
@@ -112,0 +113,2 @@
+  runBlockers?: (args: BlockerFnArgs) => Promise<boolean>
+  waitForNavigation?: () => Promise<boolean> | undefined
@@ -134,0 +137,11 @@
+    const isPushOrReplace =
+      actionInfo.type === 'PUSH' || actionInfo.type === 'REPLACE'
+
+    if (typeof document !== 'undefined' && isPushOrReplace) {
+      const pendingNavigation = opts.waitForNavigation?.()
+      if (pendingNavigation && !(await pendingNavigation)) {
+        opts.onBlocked?.()
+        return
+      }
+    }
+
@@ -141,11 +154,8 @@
-    const isPushOrReplace =
-      actionInfo.type === 'PUSH' || actionInfo.type === 'REPLACE'
-    if (typeof document !== 'undefined' && blockers.length && isPushOrReplace) {
-      for (const blocker of blockers) {
-        const nextLocation = parseHref(actionInfo.path, actionInfo.state)
-        const isBlocked = await blocker.blockerFn({
-          currentLocation: location,
-          nextLocation,
-          action: actionInfo.type,
-        })
-        if (isBlocked) {
+    if (typeof document !== 'undefined' && isPushOrReplace) {
+      const blockerArgs: BlockerFnArgs = {
+        currentLocation: location,
+        nextLocation: parseHref(actionInfo.path, actionInfo.state),
+        action: actionInfo.type,
+      }
+      if (opts.runBlockers && blockers.length > 0) {
+        if (await opts.runBlockers(blockerArgs)) {
@@ -154,0 +165,13 @@
+      } else {
+        for (const blocker of blockers) {
+          if (await blocker.blockerFn(blockerArgs)) {
+            opts.onBlocked?.()
+            return
+          }
+        }
+      }
+
+      const pendingNavigation = opts.waitForNavigation?.()
+      if (pendingNavigation && !(await pendingNavigation)) {
+        opts.onBlocked?.()
+        return
@@ -324 +347,11 @@
-  let ignoreNextPop = false
+  type PopTransactionResult = 'restored' | 'committed' | 'fallback'
+  let popTransaction:
+    | {
+        origin: HistoryLocation
+        target: HistoryLocation
+        phase: 'deciding' | 'rolling-back' | 'rolling-back-fallback'
+        sawGo: boolean
+        completion: Promise<PopTransactionResult>
+        finish: (result: PopTransactionResult) => void
+      }
+    | undefined
@@ -404,4 +437,34 @@
-  const onPushPopEvent = async () => {
-    if (ignoreNextPop) {
-      ignoreNextPop = false
-      return
+  const sameRouterEntry = (a: HistoryLocation, b: HistoryLocation) => {
+    const aIndex = a.state[stateIndexKey]
+    const bIndex = b.state[stateIndexKey]
+    const aKey = a.state.__TSR_key ?? a.state.key
+    const bKey = b.state.__TSR_key ?? b.state.key
+    return (
+      Number.isFinite(aIndex) &&
+      Number.isFinite(bIndex) &&
+      typeof aKey === 'string' &&
+      a.href === b.href &&
+      aIndex === bIndex &&
+      aKey === bKey
+    )
+  }
+
+  let blockerDecisionPending = false
+  const runBlockers = async (args: BlockerFnArgs) => {
+    const currentBlockers = _getBlockers()
+    if (
+      typeof document === 'undefined' ||
+      currentBlockers.length === 0
+    ) {
+      return false
+    }
+    if (blockerDecisionPending) return true
+
+    blockerDecisionPending = true
+    try {
+      for (const blocker of currentBlockers) {
+        if (await blocker.blockerFn(args)) return true
+      }
+      return false
+    } finally {
+      blockerDecisionPending = false
@@ -408,0 +472 @@
+  }
@@ -410,6 +474,20 @@
-    const nextLocation = parseLocation()
-    const delta =
-      nextLocation.state[stateIndexKey] - currentLocation.state[stateIndexKey]
-    const isForward = delta === 1
-    const isBack = delta === -1
-    const isGo = (!isForward && !isBack) || nextPopIsGo
+  const finishPopTransaction = (
+    transaction: NonNullable<typeof popTransaction>,
+    result: PopTransactionResult,
+  ) => {
+    if (popTransaction !== transaction) return
+    popTransaction = undefined
+    transaction.finish(result)
+  }
+
+  const waitForNavigation = () => {
+    const transaction = popTransaction
+    if (!transaction) return undefined
+    const owner = transaction.origin
+    return transaction.completion.then(
+      (result) => result === 'restored' && sameRouterEntry(currentLocation, owner),
+    )
+  }
+
+  const onPushPopEvent = async () => {
+    const popWasGo = nextPopIsGo
@@ -416,0 +495,3 @@
+    const nextLocation = parseLocation()
+    const skipBlocker = skipBlockerNextPop
+    skipBlockerNextPop = false
@@ -418,9 +499,7 @@
-    const action = isGo ? 'GO' : isBack ? 'BACK' : 'FORWARD'
-    const notify: SubscriberHistoryAction = isGo
-      ? {
-          type: 'GO',
-          index: delta,
-        }
-      : {
-          type: isBack ? 'BACK' : 'FORWARD',
-        }
+    const transaction = popTransaction
+    if (transaction) {
+      transaction.target = nextLocation
+      transaction.sawGo ||= popWasGo
+      if (transaction.phase === 'deciding') {
+        return
+      }
@@ -428,17 +507,72 @@
-    if (skipBlockerNextPop) {
-      skipBlockerNextPop = false
-    } else {
-      const blockers = _getBlockers()
-      if (typeof document !== 'undefined' && blockers.length) {
-        for (const blocker of blockers) {
-          const isBlocked = await blocker.blockerFn({
-            currentLocation,
-            nextLocation,
-            action,
-          })
-          if (isBlocked) {
-            ignoreNextPop = true
-            win.history.go(1)
-            history.notify(notify)
-            return
-          }
+      if (sameRouterEntry(nextLocation, transaction.origin)) {
+        currentLocation = nextLocation
+        finishPopTransaction(transaction, 'restored')
+        return
+      }
+
+      if (transaction.phase === 'rolling-back-fallback') {
+        finishPopTransaction(transaction, 'fallback')
+        return
+      }
+
+      const rollbackDelta =
+        transaction.origin.state[stateIndexKey] -
+        nextLocation.state[stateIndexKey]
+      if (Number.isFinite(rollbackDelta) && rollbackDelta !== 0) {
+        win.history.go(rollbackDelta)
+      } else {
+        transaction.phase = 'rolling-back-fallback'
+        win.history.go(1)
+      }
+      return
+    }
+
+    const origin = currentLocation
+    let finish = (_result: PopTransactionResult) => {}
+    const completion = new Promise<PopTransactionResult>((resolve) => {
+      finish = resolve
+    })
+    const pendingTransaction: NonNullable<typeof popTransaction> = {
+      origin,
+      target: nextLocation,
+      phase: 'deciding',
+      sawGo: popWasGo,
+      completion,
+      finish,
+    }
+    popTransaction = pendingTransaction
+
+    const navigation = () => {
+      const target = pendingTransaction.target
+      const delta =
+        target.state[stateIndexKey] - origin.state[stateIndexKey]
+      const isForward = delta === 1
+      const isBack = delta === -1
+      const isGo = pendingTransaction.sawGo || (!isForward && !isBack)
+      const action: HistoryAction = isGo ? 'GO' : isBack ? 'BACK' : 'FORWARD'
+      const notify: SubscriberHistoryAction = isGo
+        ? { type: 'GO', index: delta }
+        : { type: isBack ? 'BACK' : 'FORWARD' }
+      return { action, delta, notify, target }
+    }
+
+    if (!skipBlocker) {
+      const { action, target } = navigation()
+      const isBlocked = await runBlockers({
+        currentLocation: origin,
+        nextLocation: target,
+        action,
+      })
+      if (popTransaction !== pendingTransaction) return
+      if (isBlocked) {
+        const { delta, target } = navigation()
+        if (sameRouterEntry(target, origin)) {
+          currentLocation = target
+          finishPopTransaction(pendingTransaction, 'restored')
+          return
+        } else if (Number.isFinite(delta) && delta !== 0) {
+          pendingTransaction.phase = 'rolling-back'
+          win.history.go(-delta)
+        } else {
+          pendingTransaction.phase = 'rolling-back-fallback'
+          win.history.go(1)
@@ -445,0 +580 @@
+        return
@@ -449 +584,7 @@
-    currentLocation = parseLocation()
+    const { notify, target } = navigation()
+    if (sameRouterEntry(target, origin)) {
+      currentLocation = target
+      finishPopTransaction(pendingTransaction, 'restored')
+      return
+    }
+    currentLocation = target
@@ -450,0 +592 @@
+    finishPopTransaction(pendingTransaction, 'committed')
@@ -509,0 +652 @@
+      if (popTransaction) finishPopTransaction(popTransaction, 'fallback')
@@ -525,0 +669,2 @@
+    runBlockers,
+    waitForNavigation,
